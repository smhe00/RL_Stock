# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# Data / experiment artifacts (EXECUTION_SPEC §55: runs 内容不入库)
# 注意：锚定仓库根，避免误忽略 src/china_etf/data/（loader 研究序列模块）
/data/
/logs/
/runs/
/tmp/

# Local secrets / overrides
*.local.yaml
*.local.json
*.local.toml
config.json

# Local Codex reviewer runtime (operational logs/state/results never enter Git)
/runtime/local_reviewer/

# Web Fetch Bridge V1 runtime (logs/state never enter Git); local operator config
# (dedicated ChatGPT conversation URL) is already ignored via *.local.toml.
/runtime/web_fetch_bridge/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
