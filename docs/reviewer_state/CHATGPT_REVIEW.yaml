protocol_version: 2
actor: chatgpt_reviewer
handoff_id: G4_NON_RL_HORSE_RACE_FINAL_CORRECTIONS_001
state: REVISIONS_REQUIRED

reviewed_packet:
  path: docs/review_packets/GATE_4_NON_RL_HORSE_RACE_FINAL_CORRECTIONS.md
  commit: 59ea96240a165a5e2cb9f73455f790a2ff070aff

code_commit: 4ace12fb3ff3633dfa961a8eb69c8c6ae4629b71

decision: TARGETED_FINALIZATION_REQUIRED

response:
  path: docs/reviewer_responses/GATE_4_NON_RL_HORSE_RACE_FINAL_CORRECTIONS_REVIEWER_RESPONSE.md

passed:
  - F1_HRP_CLUSTER_VARIANCE_DIRECTION
  - F2_MIN_CVAR_RU_OBJECTIVE
  - F3_ERC_RAW_TOLERANCE_1E3
  - F5_ACTUAL_EXECUTION_DATE_PARITY
  - NO_RL_RETRAINING

blocked:
  - F4_ERC_HRP_CONSTRAINED_LABELING
  - F6_EXACT_STITCHED_DIAGNOSTICS
  - PACKET_ARTIFACT_CONSISTENCY

authorized_next:
  - GATE_4_NON_RL_HORSE_RACE_FINALIZATION

forbidden_next:
  - RL_RETRAINING
  - GATE_4_10_SEED_FORMAL
  - FEATURE_ABLATION_RUNS
  - FEATURE_DATA_READY_EXPANSION
  - OPTUNA
  - HYPERPARAMETER_SWEEP
  - TEST_INFORMED_SELECTION
  - THEME_SLEEVE
  - QMT_LIVE
  - SOUTHBOUND_EXECUTION

notes:
  - HRP cluster-variance allocation direction is corrected; the previous false high-return HRP result disappears after the fix.
  - MinimumCVaR now uses the Rockafellar-Uryasev empirical objective and independent ES validation; the old result is superseded.
  - ERC raw solver again meets the <=1e-3 contribution-deviation target.
  - Actual rollout execution dates are now recorded from st.t_next and compared directly with the frozen 475-date Test mask.
  - ERC and HRP are still solved canonically first and only then passed through _proj_constrained; overlay=0 proves pre-overlay feasibility, not a genuinely constrained ERC/HRP optimum. Relabel as project-projected variants or implement true constrained formulations.
  - waterfill_proj should be described as the frozen project feasibility projection contract, not an exact Euclidean projection unless proven.
  - Stitched diagnostics still omit actual_traded_notional, total_cost_over_traded_notional and risk_overlay_mean_l1_raw_to_post even though rollout provides them.
  - Stitched total_turnover is currently approximated as mean_turnover*n_eval rather than summed from exact per-fold total_turnover; totals and weighted means must be aggregated from the actual rollout fields.
  - fallback_count must be auditable rather than a hard-coded unexplained zero.
  - Packet table has an EqualWeight overlay inconsistency versus the tracked artifact; regenerate packet summary from the final artifact.
  - Only deterministic non-RL cleanup/rerun is authorized. Existing TD3/SAC/PPO remain historical pre-correction references.

updated_at: 2026-08-09T06:28:43+08:00
