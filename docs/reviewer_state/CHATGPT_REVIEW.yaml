protocol_version: 2
actor: chatgpt_reviewer
handoff_id: CORRECTED_F0_RL_EXECUTION_HARNESS_FINALIZATION_001
state: REVISIONS_REQUIRED

reviewed_packet:
  path: docs/review_packets/CORRECTED_F0_RL_EXECUTION_HARNESS_FINALIZATION.md
  commit: 361dbe535db2b5d647c143cc93bc06800f974956

code_commit: 765ccacb8350e35efc25721f74f465e3be4a6f7d

decision: TARGETED_PUBLICATION_METRIC_BINDING_REQUIRED

response:
  path: docs/reviewer_responses/CORRECTED_F0_RL_EXECUTION_HARNESS_FINALIZATION_REVIEWER_RESPONSE.md

passed:
  - F1_ALGO_NAME_CLASS_MISMATCH_REJECTED
  - F2_MISSING_N_EVAL_STEPS_FAIL_CLOSED
  - F3_RAW_SERIES_LENGTH_AND_WEIGHT_SHAPE
  - F4_METRIC_SEED_KEYS_AND_PROJECT_ALGO_COMPLETENESS_IN_EVALUATOR
  - F5_PARETO_ALL_FINITE_REQUIREMENT
  - F6_RUN_LEVEL_CONFIG_SHA_AND_INVARIANT_BEFORE_EVALUATION_CORE
  - NO_RL_TRAINING

blocked:
  - P1_GO_NO_GO_METRICS_NOT_DERIVED_FROM_VALIDATED_RAW_RUN_RESULTS
  - P2_STOP_CONDITIONS_NOT_DERIVED_FROM_ACTUAL_RUN_RECORDS
  - P3_SYNTHETIC_PUBLICATION_PROOF_NOT_USING_REAL_CANONICAL_TEST_MASK

authorized_next:
  - CORRECTED_F0_RL_EXECUTION_PUBLICATION_BINDING

forbidden_next:
  - RL_RETRAINING
  - CORRECTED_F0_RL_3SEED
  - GATE_4_10_SEED_FORMAL
  - OPTUNA
  - HYPERPARAMETER_SWEEP
  - TEST_INFORMED_ALGO_SELECTION
  - TEST_INFORMED_FEATURE_SELECTION
  - FEATURE_SET_CHANGE_FROM_CURRENT_TEST_RESULTS
  - FEATURE_DATA_READY_EXPANSION
  - F2_F3_REAL_MACRO_RUN
  - QMT_LIVE
  - SOUTHBOUND_EXECUTION

notes:
  - F1-F6 are substantially closed; the remaining issue is publication integrity rather than research design.
  - finalize_publish currently validates raw results but evaluates an independently supplied per_algo_stitched object. A passing test uses zero raw net_returns while supplying positive stitched return/Sharpe and still publishes, proving the disconnect.
  - Canonical stitched active_day_annualized_return, Sharpe, MaxDD, Calmar and seed-level decision inputs must be recomputed directly from ordered F1-F4 raw net_returns inside the publication path.
  - Stop violations must be derived from actual run evidence (save/load determinism, NaN/Inf, negative cash, finite series/metrics); missing evidence must fail closed.
  - Caller-supplied stitched summaries may be diagnostic only; if retained, mismatch to canonical recomputation must fail closed and they must never be authoritative for GO/NO-GO.
  - The final synthetic publication proof should use the repository's real folds and exact_test_mask rather than an arbitrary continuous business-day calendar.
  - CORRECTED_F0_RL_EXECUTION_PUBLICATION_BINDING is no-training closeout only. If it passes without a new material defect, intended next gate is CORRECTED_F0_RL_3SEED.

updated_at: 2026-08-09T11:41:00+08:00
