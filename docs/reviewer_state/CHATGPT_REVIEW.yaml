protocol_version: 2
actor: chatgpt_reviewer
handoff_id: G4_FEATURE_ABLATION_RUNS_CORRECTIONS_001
state: REVISIONS_REQUIRED

reviewed_packet:
  path: docs/review_packets/GATE_4_FEATURE_ABLATION_RUNS_CORRECTIONS.md
  commit: 0dfc536b8728605f42b81007e72f0d17256e3822

code_commit: 520d13ad1252d7ef73b57ec6175b349bd06b6510

decision: TARGETED_STATISTICAL_FINALIZATION_REQUIRED

response:
  path: docs/reviewer_responses/GATE_4_FEATURE_ABLATION_RUNS_CORRECTIONS_REVIEWER_RESPONSE.md

passed:
  - A2_NON_RL_DIAGNOSTIC_SCOPE
  - A5_COMPOSITE_IMPORTANCE_SCORE_REMOVED
  - NO_RL_RETRAINING
  - NO_10_SEED
  - NO_OPTUNA_OR_SWEEP

partial_pass:
  - TRAIN_FIT_VALIDATION_APPLY_MECHANICS
  - TIME_SERIES_RESAMPLING_INFRASTRUCTURE

blocked:
  - B1_GLOBAL_475_TEST_QUARANTINE_SEMANTICS
  - B2_INVALID_MEDIAN_P_THEN_HOLM_BH
  - B3_INVALID_AGGREGATE_BOOTSTRAP_CI_LABEL
  - B4_INVALID_BOOTSTRAP_NULL_P_VALUE
  - B5_REDUCED_F0_PROXY_OVERCLAIM

authorized_next:
  - GATE_4_FEATURE_ABLATION_RUNS_STAT_FINALIZATION

forbidden_next:
  - RL_RETRAINING
  - GATE_4_10_SEED_FORMAL
  - OPTUNA
  - HYPERPARAMETER_SWEEP
  - TEST_INFORMED_FEATURE_SELECTION
  - FEATURE_DATA_READY_EXPANSION
  - F2_F3_REAL_MACRO_RUN
  - FEATURE_SET_CHANGE_FROM_CURRENT_TEST_RESULTS
  - QMT_LIVE
  - SOUTHBOUND_EXECUTION

notes:
  - Later expanding folds reuse earlier frozen Test dates in train, so current packet cannot claim the union 475-day Test panel is globally quarantined.
  - Median of per-fold Mann-Whitney p-values is not a calibrated p-value; Holm/BH on that median does not provide valid family-wise/FDR control.
  - Median lower/upper endpoints of per-fold bootstrap intervals are not an aggregate 95% confidence interval.
  - block_bootstrap_ci p_bs compares the empirical bootstrap distribution to the observed statistic rather than a null-centered distribution and should be removed or replaced.
  - BLOCK_LEN=20 is incorrectly described as the longest rolling window; F1 contains 60-day windows. Add block-length sensitivity or a justified automatic rule.
  - Residualization uses only a 10-variable reduced F0 market proxy, not the full 104-dimensional F0 observation contract; conclusions must be narrowed accordingly.
  - Current evidence can support only a descriptive statement that F1 lacks a strong stable monotonic association in development data; it cannot prove no F1 predictive value or refute the previously observed Test association independently.
  - Frozen F1 candidate set remains unchanged; no feature may be added/removed because of current Test results.

updated_at: 2026-08-09T09:03:00+08:00
